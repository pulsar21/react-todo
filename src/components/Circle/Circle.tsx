import {FC} from "react";
import "../../assets/styles/circle/circle.scss";

interface ICircleProps {

};

const Circle: FC<ICircleProps> = (props) => {
    const {

    } = props;

    return (
        <div>
            Circle
        </div>
    );
};

export default Circle;